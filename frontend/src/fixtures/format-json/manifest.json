{
  "name": "Format JSON",
  "description": "Format JSON document",
  "pipe": {
    "exec": "python3",
    "args": ["-m", "json.tool"],
    "stdin": "{{args.content}}"
  },
  "output": "replace",
  "tags": ["format", "json", "prettify", "pretty"]
}
